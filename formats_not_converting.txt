1. All codes should have different background: 
[
    `It's background should be formatted`
    ```It's background should also be formatted```
    (
        Syntax highlighting(Markdown extension)
        [
            ```js
            var foo = function (bar) {
                return bar++;
            };
            ``` 
        ]
    )
]

2. Start numbering with offset:
[
    57. foo
    1. bar

    should be converted to:

    57. foo
    58. bar

    But, getting converted to:

    1. foo
    2. bar
]

3. Tables is not getting converted.[

    | Option | Description                                                               |
    | ------ | ------------------------------------------------------------------------- |
    | data   | path to data files to supply the data that will be passed into templates. |
    | engine | engine to be used for processing templates. Handlebars is the default.    |
    | ext    | extension to be used for dest files.                                      |

]

4. Blockquates border is not formatted. [It will be done with CSS]

[
    > Blockquotes can also be nested...
    >
    > > ...by using additional greater-than signs right next to each other...
    > >
    > > > ...or with spaces between arrows.
]

5. Auto-converted link.
[
    Autoconverted link https://github.com/nodeca/pica
]

6. Emojies are not being converted.[Some are being converted, but not all !!]
[
    Classic markup: :wink: :crush: :cry: :tear: :laughing: :yum:
    Shortcuts (emoticons): :-) :-( 8-) ;)
]

7. Subscript/Subscript not being converted.
[
    19^th^
    H~2~O
]

8. Inserted text in not rendering
[
    <ins>
    ++Inserted text++
]

9. Marked text in not being converted.
[
    <mark>
    ==Marked text==
]

10. Footnote link is not rendering. (like wikipedia, [1], [2] with supscript and anchor tag)
[
    Footnote 1 link[^first].
    Footnote 2 link[^second].

    (Inline footnote^[Text of inline footnote] definition.)
]

11. definition list is not rendering.(https://markdown-it.github.io/)
[
    Term 1

    : Definition 1
    with lazy continuation.

    Term 2 with _inline markup_

    : Definition 2

            { some code, part of Definition 2 }

        Third paragraph of definition 2.

    _Compact style:_

    Term 1
    ~ Definition 1

    Term 2
    ~ Definition 2a
    ~ Definition 2b
]

12. Abbreviations are also not rendering.(Like tooltip)
[
This is HTML abbreviation example.

It converts "HTML", but keep intact partial entries like "xxxHTMLyyy" and so on.

\*[HTML]: Hyper Text Markup Language
]

13. Equations.
[
    (Chemistry)
    $\ce{OH-}$ ions as follows :
    $$\ce{O2 + 4H2O + 4e- -> 4OH-}$$
    The over all reaction of the miniature cell will be :
    $$\ce{2Fe(s) + O2(g) + 4H+(aq) -> Fe^2+(aq) + 2H2O(l)}$$

    (Mathematics)
    $$
    \int_0^\infty \frac{x^3}{e^x-1}\,dx = \frac{\pi^4}{15}
    $$
]

